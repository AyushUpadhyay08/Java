/*A shop will give discount of 10% if the cost of purchased quantity is more than 1000.
Ask user for quantity
Suppose, one unit will cost 100.
Judge and print total cost for user.*/
import java.util.*;
class J
{
    public static void main(String args[])
    {
        Scanner sc = new Scanner(System.in);
        System.out.println("Enter the number of quantity");
        int x = sc.nextInt();
        int total=x*100;
        if(total>1000)
            System.out.println("Total Price is: " + total + " but after disscount: " + (total - (total * 0.1)));
        else
        {
            System.out.println("No disscount");
            System.out.println("cost of purchased price: " + total);
        }
    }

}
